# H4 · Branding por tenant — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cada organización de plan pago (Pro/Business) puede personalizar su identidad visual (color de acento, logo, nombre visible, tema inicial), aplicada en la app autenticada, en páginas pre-auth por slug, con caída a la marca del producto cuando no aplica.

**Architecture:** Modelo `OrganizationBranding` OneToOne con `Organization` en la app `tenancy`. API de edición scoped por org del usuario (`/api/branding/`, admin, gateada a plan pago) + endpoint público de superficie mínima (`/api/public/branding/<slug>/`) + branding embebido en `/api/me/`. Front: composable `useBranding` que sobreescribe CSS custom properties en runtime; pantalla admin; rutas pre-auth `/o/:slug/*`.

**Tech Stack:** Django 6 + DRF, Pillow (ya instalado) para `ImageField`, MySQL. Vue 3.5 + Pinia + vue-router. Sin nuevas dependencias.

## Global Constraints

- **Backend Python invocation (Windows):** `C:/Python312/python.exe manage.py <cmd>` ejecutado desde `backend/`. Iterar tests con `--keepdb`; el gate final corre en DB fresca (`--noinput`).
- **Fuente de verdad del estado pago = servidor:** el gating de branding se decide en el backend vía `org.subscription.effective_plan.key ∈ {"pro","business"}`. El front puede ocultar/deshabilitar, pero el backend responde 403 aunque se invoque el PUT directo. Ningún camino del front habilita branding por su cuenta.
- **Aislamiento multi-tenant:** el endpoint de edición nunca acepta `organization` por body/param — siempre `request.organization` (org del usuario autenticado). El endpoint público expone **exclusivamente** `{display_name, accent_color, logo_url, default_dark}`, solo de orgs `is_active` y de plan pago; cualquier otro caso → **404** (nunca revela existencia ni datos sensibles).
- **Estética front:** hairline azul eléctrico; tokens neutros (fondos/bordes/texto) NO se personalizan, solo el acento y sus derivados. Copys de UI en **voseo** (registrá, personalizá, guardá).
- **Validación imagen:** logo ≤ 512 KB; formatos PNG/JPEG/WebP; **SVG no permitido** (Pillow no lo valida como imagen → riesgo de markup ejecutable).
- **Validación color:** patrón hex exacto `^#[0-9A-Fa-f]{6}$`; vacío = usar acento del producto.
- **Sin exención de middleware:** el endpoint público funciona para anónimos sin tocar `OrganizationMiddleware.EXEMPT_PREFIXES` (el middleware solo bloquea usuarios autenticados sin org).

## File Structure

Backend (app `tenancy`):
- `backend/tenancy/models.py` — MODIFICAR: agregar `OrganizationBranding` + validator de color.
- `backend/tenancy/branding_serializers.py` — CREAR: serializer de edición + helper `branding_payload(org, request)`.
- `backend/tenancy/branding_views.py` — CREAR: `BrandingView` (admin GET/PUT) + `PublicBrandingView` (GET por slug).
- `backend/tenancy/branding_urls.py` — CREAR: ruta admin `/api/branding/`.
- `backend/tenancy/branding_services.py` — CREAR: `branding_enabled(org)`.
- `backend/tenancy/migrations/000X_organizationbranding.py` — CREAR (via makemigrations).
- `backend/config/urls.py` — MODIFICAR: incluir `tenancy.branding_urls` + ruta pública.
- `backend/config/views.py` — MODIFICAR: `me()` incluye `branding`.
- `backend/tenancy/tests_branding.py` — CREAR: unit + API.
- `backend/tenancy/tests_branding_adversarial.py` — CREAR: suite adversarial.

Frontend:
- `frontend/src/composables/useBranding.js` — CREAR: `applyBranding` / `clearBranding`.
- `frontend/src/api/branding.api.js` — CREAR: cliente admin + público.
- `frontend/src/stores/auth.store.js` — MODIFICAR: aplicar branding tras `loadMe`.
- `frontend/src/components/AppTopBar.vue` — MODIFICAR: logo + display_name.
- `frontend/src/views/dashboards/AdminBranding.vue` — CREAR: pantalla admin.
- `frontend/src/router/index.js` — MODIFICAR: ruta `/admin/branding` + rutas `/o/:slug/*`.
- `frontend/src/views/public/OrgLoginView.vue` — CREAR: login branded por slug (envuelve el login existente).

---

### Task 1: Modelo `OrganizationBranding` + validación de color

**Files:**
- Modify: `backend/tenancy/models.py`
- Create: `backend/tenancy/migrations/000X_organizationbranding.py` (via makemigrations)
- Test: `backend/tenancy/tests_branding.py`

**Interfaces:**
- Produces: `OrganizationBranding` con campos `organization` (OneToOne, related_name="branding"), `display_name` (CharField 80), `accent_color` (CharField 7), `logo` (ImageField), `default_dark` (BooleanField), `updated_at`. Propiedad `effective_display_name` → `display_name or organization.name`. Validator modular `HEX_COLOR_VALIDATOR`.

- [ ] **Step 1: Write the failing test**

En `backend/tenancy/tests_branding.py`:

```python
from django.core.exceptions import ValidationError
from django.test import TestCase

from tenancy.testing import create_org
from tenancy.models import OrganizationBranding


class BrandingModelTests(TestCase):
    def test_effective_display_name_falls_back_to_org_name(self):
        org = create_org("BR1", name="Acme SA")
        b = OrganizationBranding.objects.create(organization=org)
        self.assertEqual(b.effective_display_name, "Acme SA")
        b.display_name = "Acme Support"
        self.assertEqual(b.effective_display_name, "Acme Support")

    def test_accent_color_rejects_non_hex(self):
        org = create_org("BR2")
        b = OrganizationBranding(organization=org, accent_color="red")
        with self.assertRaises(ValidationError):
            b.full_clean()

    def test_accent_color_accepts_hex(self):
        org = create_org("BR3")
        b = OrganizationBranding(organization=org, accent_color="#0038FF")
        b.full_clean()  # no debe levantar
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && C:/Python312/python.exe manage.py test tenancy.tests_branding --keepdb -v2`
Expected: FAIL con `ImportError` / `cannot import name 'OrganizationBranding'`.

- [ ] **Step 3: Write minimal implementation**

En `backend/tenancy/models.py`, agregar al final:

```python
from django.core.validators import RegexValidator

HEX_COLOR_VALIDATOR = RegexValidator(
    regex=r"^#[0-9A-Fa-f]{6}$",
    message="El color debe ser hex de 6 dígitos, ej. #0038FF.",
)


class OrganizationBranding(models.Model):
    organization = models.OneToOneField(
        Organization, on_delete=models.CASCADE, related_name="branding"
    )
    display_name = models.CharField(max_length=80, blank=True)
    accent_color = models.CharField(max_length=7, blank=True, validators=[HEX_COLOR_VALIDATOR])
    logo = models.ImageField(upload_to="branding/logos/", blank=True, null=True)
    default_dark = models.BooleanField(default=False)
    updated_at = models.DateTimeField(auto_now=True)

    @property
    def effective_display_name(self):
        return self.display_name or self.organization.name

    def __str__(self):
        return f"Branding · {self.organization.name}"
```

(`Organization` ya está definido arriba en el mismo archivo; usar el símbolo directo, no el string.)

- [ ] **Step 4: Crear la migración**

Run: `cd backend && C:/Python312/python.exe manage.py makemigrations tenancy`
Expected: crea `000X_organizationbranding.py`. Revisar que solo agregue el modelo nuevo.

- [ ] **Step 5: Run test to verify it passes**

Run: `cd backend && C:/Python312/python.exe manage.py test tenancy.tests_branding --keepdb -v2`
Expected: PASS (3 tests).

- [ ] **Step 6: Commit**

```bash
git add backend/tenancy/models.py backend/tenancy/migrations backend/tenancy/tests_branding.py
git commit -m "feat(tenancy): modelo OrganizationBranding + validación hex de color"
```

---

### Task 2: Serializer + API admin `/api/branding/` (GET/PUT, gateada, scoped)

**Files:**
- Create: `backend/tenancy/branding_serializers.py`
- Create: `backend/tenancy/branding_services.py`
- Create: `backend/tenancy/branding_views.py`
- Create: `backend/tenancy/branding_urls.py`
- Modify: `backend/config/urls.py`
- Test: `backend/tenancy/tests_branding.py` (agregar clase)

**Interfaces:**
- Consumes: `OrganizationBranding` (Task 1), `tickets_t.permissions.IsAdmin`, `request.organization` (middleware).
- Produces:
  - `branding_services.branding_enabled(org) -> bool` (True si `effective_plan.key ∈ {"pro","business"}`).
  - `branding_serializers.BrandingSerializer` (campos: `display_name`, `accent_color`, `logo`, `default_dark`; read-only extra `logo_url`, `effective_display_name`).
  - `branding_serializers.branding_payload(org, request) -> dict | None` (dict con `{display_name, accent_color, logo_url, default_dark}` si `branding_enabled` y existe branding; si no, `None`).
  - `BrandingView` en `/api/branding/` (GET/PUT, `IsAuthenticated + IsAdmin`).

- [ ] **Step 1: Write the failing test**

Agregar a `backend/tenancy/tests_branding.py`:

```python
import io

from django.contrib.auth import get_user_model
from rest_framework.test import APIClient

from billing.models import Plan, Subscription

User = get_user_model()


def _png_bytes():
    from PIL import Image
    buf = io.BytesIO()
    Image.new("RGB", (4, 4), "#0038FF").save(buf, format="PNG")
    buf.seek(0)
    buf.name = "logo.png"
    return buf


class BrandingAdminApiTests(TestCase):
    def setUp(self):
        self.org = create_org("BRA")  # provision_test_org -> Business (plan pago)
        self.admin = User.objects.create_user("bra_admin", email="a@x.com",
                                               role="ADMIN", organization=self.org)
        self.c = APIClient(); self.c.force_authenticate(self.admin)

    def test_get_returns_defaults_without_persisting(self):
        r = self.c.get("/api/branding/")
        self.assertEqual(r.status_code, 200)
        self.assertEqual(r.data["accent_color"], "")
        self.assertEqual(r.data["effective_display_name"], self.org.name)
        from tenancy.models import OrganizationBranding
        self.assertEqual(OrganizationBranding.objects.count(), 0)

    def test_put_saves_branding(self):
        r = self.c.put("/api/branding/", {"display_name": "Acme Support",
                                          "accent_color": "#123456",
                                          "default_dark": True}, format="json")
        self.assertEqual(r.status_code, 200)
        self.assertEqual(r.data["accent_color"], "#123456")
        self.assertTrue(r.data["default_dark"])

    def test_put_rejects_bad_color(self):
        r = self.c.put("/api/branding/", {"accent_color": "red"}, format="json")
        self.assertEqual(r.status_code, 400)

    def test_free_plan_cannot_edit(self):
        sub = self.org.subscription
        sub.plan = Plan.objects.get(key="free")
        sub.status = Subscription.Status.ACTIVE
        sub.save()
        r = self.c.put("/api/branding/", {"accent_color": "#123456"}, format="json")
        self.assertEqual(r.status_code, 403)

    def test_non_admin_cannot_edit(self):
        agent = User.objects.create_user("bra_ag", email="ag@x.com",
                                         role="AGENT", organization=self.org)
        c = APIClient(); c.force_authenticate(agent)
        r = c.put("/api/branding/", {"accent_color": "#123456"}, format="json")
        self.assertEqual(r.status_code, 403)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && C:/Python312/python.exe manage.py test tenancy.tests_branding.BrandingAdminApiTests --keepdb -v2`
Expected: FAIL (404 en `/api/branding/`, ruta inexistente).

- [ ] **Step 3: Implementar `branding_services.py`**

`backend/tenancy/branding_services.py`:

```python
def branding_enabled(org):
    """True si la org tiene plan pago (Pro/Business) según el estado efectivo."""
    if org is None:
        return False
    sub = getattr(org, "subscription", None)
    if sub is None:
        return False
    return sub.effective_plan.key in ("pro", "business")
```

- [ ] **Step 4: Implementar `branding_serializers.py`**

`backend/tenancy/branding_serializers.py`:

```python
from rest_framework import serializers

from .models import OrganizationBranding

_MAX_LOGO_BYTES = 512 * 1024
_ALLOWED_FORMATS = {"PNG", "JPEG", "WEBP"}


class BrandingSerializer(serializers.ModelSerializer):
    logo_url = serializers.SerializerMethodField()
    effective_display_name = serializers.SerializerMethodField()

    class Meta:
        model = OrganizationBranding
        fields = ["display_name", "accent_color", "logo", "default_dark",
                  "logo_url", "effective_display_name"]
        extra_kwargs = {"logo": {"write_only": True, "required": False}}

    def get_logo_url(self, obj):
        if not obj.pk or not obj.logo:
            return None
        request = self.context.get("request")
        url = obj.logo.url
        return request.build_absolute_uri(url) if request else url

    def get_effective_display_name(self, obj):
        return obj.effective_display_name if obj.pk else \
            self.context["organization"].name

    def validate_logo(self, value):
        if value is None:
            return value
        if value.size > _MAX_LOGO_BYTES:
            raise serializers.ValidationError("El logo no puede superar 512 KB.")
        fmt = getattr(getattr(value, "image", None), "format", None)
        if fmt not in _ALLOWED_FORMATS:
            raise serializers.ValidationError("Formato no soportado (usá PNG, JPEG o WebP).")
        return value


def branding_payload(org, request):
    """Payload público mínimo o None si no aplica (sin plan pago o sin branding)."""
    from .branding_services import branding_enabled
    if not branding_enabled(org):
        return None
    branding = getattr(org, "branding", None)
    if branding is None:
        return None
    logo_url = None
    if branding.logo:
        logo_url = request.build_absolute_uri(branding.logo.url) if request else branding.logo.url
    return {
        "display_name": branding.effective_display_name,
        "accent_color": branding.accent_color,
        "logo_url": logo_url,
        "default_dark": branding.default_dark,
    }
```

Nota sobre `validate_logo`: DRF puebla `value.image` (Pillow) para `ImageField`; si el archivo no es imagen válida, el `ImageField` ya levanta error antes de este validador.

- [ ] **Step 5: Implementar `branding_views.py` (admin)**

`backend/tenancy/branding_views.py`:

```python
from rest_framework.parsers import FormParser, JSONParser, MultiPartParser
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response
from rest_framework.views import APIView

from tickets_t.permissions import IsAdmin
from .branding_serializers import BrandingSerializer
from .branding_services import branding_enabled
from .models import OrganizationBranding


class BrandingView(APIView):
    permission_classes = [IsAuthenticated, IsAdmin]
    parser_classes = [MultiPartParser, FormParser, JSONParser]

    def get(self, request):
        org = request.organization
        branding = getattr(org, "branding", None)
        if branding is None:
            branding = OrganizationBranding(organization=org)  # sin persistir
        ser = BrandingSerializer(branding, context={"request": request, "organization": org})
        return Response(ser.data)

    def put(self, request):
        org = request.organization
        if not branding_enabled(org):
            return Response(
                {"detail": "El branding es una función de los planes Pro y Business."},
                status=403)
        branding, _ = OrganizationBranding.objects.get_or_create(organization=org)
        ser = BrandingSerializer(branding, data=request.data, partial=True,
                                 context={"request": request, "organization": org})
        ser.is_valid(raise_exception=True)
        ser.save()
        return Response(ser.data)
```

- [ ] **Step 6: Rutas**

`backend/tenancy/branding_urls.py`:

```python
from django.urls import path
from .branding_views import BrandingView

urlpatterns = [
    path("", BrandingView.as_view(), name="branding"),
]
```

En `backend/config/urls.py`, dentro de `urlpatterns`, agregar junto a los otros `api/`:

```python
    path("api/branding/", include("tenancy.branding_urls")),
```

- [ ] **Step 7: Run test to verify it passes**

Run: `cd backend && C:/Python312/python.exe manage.py test tenancy.tests_branding.BrandingAdminApiTests --keepdb -v2`
Expected: PASS (5 tests).

- [ ] **Step 8: Commit**

```bash
git add backend/tenancy/branding_serializers.py backend/tenancy/branding_services.py backend/tenancy/branding_views.py backend/tenancy/branding_urls.py backend/config/urls.py backend/tenancy/tests_branding.py
git commit -m "feat(tenancy): API admin /api/branding/ (GET/PUT) gateada a plan pago"
```

---

### Task 3: Endpoint público por slug + branding en `/api/me/`

**Files:**
- Modify: `backend/tenancy/branding_views.py` (agregar `PublicBrandingView`)
- Modify: `backend/config/urls.py` (ruta pública)
- Modify: `backend/config/views.py` (`me()` incluye branding)
- Test: `backend/tenancy/tests_branding.py` (agregar clase)

**Interfaces:**
- Consumes: `branding_payload(org, request)` (Task 2), `Organization` (slug), `IsAuthenticated`.
- Produces: `GET /api/public/branding/<slug>/` → 200 con payload mínimo o 404. `/api/me/` gana clave `branding` (payload o None).

- [ ] **Step 1: Write the failing test**

Agregar a `backend/tenancy/tests_branding.py`:

```python
class BrandingPublicApiTests(TestCase):
    def setUp(self):
        self.org = create_org("BRP", name="Public Co")  # Business (pago)
        from tenancy.models import OrganizationBranding
        OrganizationBranding.objects.create(
            organization=self.org, display_name="Public Support", accent_color="#ABCDEF")

    def test_public_returns_minimal_branding_for_paid_org(self):
        r = APIClient().get(f"/api/public/branding/{self.org.slug}/")
        self.assertEqual(r.status_code, 200)
        self.assertEqual(set(r.data.keys()),
                         {"display_name", "accent_color", "logo_url", "default_dark"})
        self.assertEqual(r.data["display_name"], "Public Support")
        self.assertEqual(r.data["accent_color"], "#ABCDEF")

    def test_public_404_for_unknown_slug(self):
        r = APIClient().get("/api/public/branding/NOPE/")
        self.assertEqual(r.status_code, 404)

    def test_public_404_for_free_org(self):
        sub = self.org.subscription
        sub.plan = Plan.objects.get(key="free")
        sub.status = Subscription.Status.ACTIVE
        sub.save()
        r = APIClient().get(f"/api/public/branding/{self.org.slug}/")
        self.assertEqual(r.status_code, 404)

    def test_public_404_for_inactive_org(self):
        self.org.is_active = False
        self.org.save()
        r = APIClient().get(f"/api/public/branding/{self.org.slug}/")
        self.assertEqual(r.status_code, 404)

    def test_me_includes_branding(self):
        admin = User.objects.create_user("brp_admin", email="a@x.com",
                                         role="ADMIN", organization=self.org)
        c = APIClient(); c.force_authenticate(admin)
        r = c.get("/api/me/")
        self.assertEqual(r.status_code, 200)
        self.assertIsNotNone(r.data["branding"])
        self.assertEqual(r.data["branding"]["accent_color"], "#ABCDEF")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && C:/Python312/python.exe manage.py test tenancy.tests_branding.BrandingPublicApiTests --keepdb -v2`
Expected: FAIL (404 ruta pública inexistente / `me` sin clave `branding`).

- [ ] **Step 3: Implementar `PublicBrandingView`**

Agregar a `backend/tenancy/branding_views.py`:

```python
from rest_framework.permissions import AllowAny

from .branding_serializers import branding_payload


class PublicBrandingView(APIView):
    """Superficie pública mínima por slug. 404 si la org no existe, está inactiva,
    o no tiene plan pago: nunca revela datos sensibles ni la existencia del tenant."""
    permission_classes = [AllowAny]

    def get(self, request, slug):
        from .models import Organization
        org = Organization.objects.filter(slug=slug.upper(), is_active=True).first()
        payload = branding_payload(org, request) if org else None
        if payload is None:
            return Response({"detail": "No encontrado."}, status=404)
        return Response(payload)
```

(El slug se guarda en mayúsculas — `Organization.save` lo normaliza — por eso `slug.upper()`.)

- [ ] **Step 4: Ruta pública en `config/urls.py`**

Agregar el import y la ruta:

```python
from tenancy.branding_views import PublicBrandingView
```

```python
    path("api/public/branding/<str:slug>/", PublicBrandingView.as_view(), name="public-branding"),
```

- [ ] **Step 5: Extender `me()` en `config/views.py`**

Modificar `me()` para incluir branding (agregar antes del `return`):

```python
    from tenancy.branding_serializers import branding_payload
    org = u.organization if u.organization_id else None
    ...
            "organization": u.organization.name if u.organization_id else None,
            "branding": branding_payload(org, request) if org else None,
```

El diccionario final queda:

```python
@api_view(["GET"])
@permission_classes([IsAuthenticated])
def me(request):
    from tenancy.branding_serializers import branding_payload
    u = request.user
    org = u.organization if u.organization_id else None
    return Response(
        {
            "id": u.id,
            "username": u.username,
            "email": u.email,
            "is_staff": u.is_staff,
            "is_superuser": u.is_superuser,
            "role": u.role,
            "organization": org.name if org else None,
            "branding": branding_payload(org, request) if org else None,
        }
    )
```

- [ ] **Step 6: Run test to verify it passes**

Run: `cd backend && C:/Python312/python.exe manage.py test tenancy.tests_branding.BrandingPublicApiTests --keepdb -v2`
Expected: PASS (5 tests).

- [ ] **Step 7: Commit**

```bash
git add backend/tenancy/branding_views.py backend/config/urls.py backend/config/views.py backend/tenancy/tests_branding.py
git commit -m "feat(tenancy): endpoint público /api/public/branding/<slug>/ + branding en /api/me/"
```

---

### Task 4: Suite adversarial

**Files:**
- Create: `backend/tenancy/tests_branding_adversarial.py`

**Interfaces:**
- Consumes: todo lo anterior. Ataca aislamiento cross-org, fuga del endpoint público, gating de plan y de rol.

- [ ] **Step 1: Write the failing test (que debe pasar ya, verificando defensas existentes)**

`backend/tenancy/tests_branding_adversarial.py`:

```python
"""Suite adversarial de branding (H4): confirma que
- una org Free no puede editar branding ni exponerlo públicamente,
- un admin de org A no puede editar/leer el branding de org B,
- el endpoint público solo expone los 4 campos de branding (sin plan, agentes, etc.),
- un no-admin no puede editar branding.
"""
from django.contrib.auth import get_user_model
from django.test import TestCase
from rest_framework.test import APIClient

from tenancy.testing import create_org
from tenancy.models import OrganizationBranding
from billing.models import Plan, Subscription

User = get_user_model()


class BrandingAdversarialTests(TestCase):
    def setUp(self):
        self.org_a = create_org("BXA", name="Org A")  # Business
        self.org_b = create_org("BXB", name="Org B")  # Business
        self.admin_a = User.objects.create_user("bxa_admin", email="a@x.com",
                                                 role="ADMIN", organization=self.org_a)
        self.c = APIClient(); self.c.force_authenticate(self.admin_a)

    def test_put_ignores_org_in_body_and_scopes_to_user_org(self):
        # aunque el body traiga otra org, el branding editado es el de la org del user
        self.c.put("/api/branding/", {"accent_color": "#111111",
                                      "organization": self.org_b.id}, format="json")
        self.assertTrue(OrganizationBranding.objects.filter(
            organization=self.org_a, accent_color="#111111").exists())
        self.assertFalse(OrganizationBranding.objects.filter(
            organization=self.org_b).exists())

    def test_admin_a_cannot_read_org_b_branding_via_admin_endpoint(self):
        OrganizationBranding.objects.create(organization=self.org_b, accent_color="#B0B0B0")
        r = self.c.get("/api/branding/")  # siempre devuelve el de la org del user (A)
        self.assertNotEqual(r.data.get("accent_color"), "#B0B0B0")

    def test_public_endpoint_leaks_only_branding_fields(self):
        OrganizationBranding.objects.create(organization=self.org_b, accent_color="#C0C0C0")
        r = APIClient().get(f"/api/public/branding/{self.org_b.slug}/")
        self.assertEqual(r.status_code, 200)
        forbidden = {"plan", "agent_count", "agent_limit", "status", "id",
                     "organization", "stripe_customer_id"}
        self.assertEqual(set(r.data.keys()) & forbidden, set())

    def test_free_org_branding_not_public(self):
        OrganizationBranding.objects.create(organization=self.org_b, accent_color="#D0D0D0")
        sub = self.org_b.subscription
        sub.plan = Plan.objects.get(key="free")
        sub.status = Subscription.Status.ACTIVE
        sub.save()
        r = APIClient().get(f"/api/public/branding/{self.org_b.slug}/")
        self.assertEqual(r.status_code, 404)

    def test_customer_cannot_edit_branding(self):
        cust = User.objects.create_user("bxa_c", email="c@x.com",
                                        role="CUSTOMER", organization=self.org_a)
        c = APIClient(); c.force_authenticate(cust)
        r = c.put("/api/branding/", {"accent_color": "#EEEEEE"}, format="json")
        self.assertEqual(r.status_code, 403)
```

- [ ] **Step 2: Run tests**

Run: `cd backend && C:/Python312/python.exe manage.py test tenancy.tests_branding_adversarial --keepdb -v2`
Expected: PASS (5 tests). Si alguno falla, es un hueco real de aislamiento → corregir la vista/serializer antes de continuar.

- [ ] **Step 3: Commit**

```bash
git add backend/tenancy/tests_branding_adversarial.py
git commit -m "test(tenancy): suite adversarial de branding (aislamiento + gating)"
```

---

### Task 5: Front — composable `useBranding` + cliente API + integración en app autenticada

**Files:**
- Create: `frontend/src/composables/useBranding.js`
- Create: `frontend/src/api/branding.api.js`
- Modify: `frontend/src/stores/auth.store.js`
- Modify: `frontend/src/components/AppTopBar.vue`

**Interfaces:**
- Consumes: `/api/me/` (branding), `useTheme` (para default_dark).
- Produces: `applyBranding(branding)` / `clearBranding()`. `auth.store.loadMe` aplica branding. TopBar muestra `logo_url` + `display_name`.

- [ ] **Step 1: Crear `useBranding.js`**

`frontend/src/composables/useBranding.js`:

```javascript
// Sobreescribe en runtime las CSS custom properties del acento a partir del
// branding de la org. Un único punto de inyección: el resto de componentes ya
// consume var(--accent) y derivados. Los tokens neutros NO se tocan.
const ACCENT_VARS = ['--accent', '--accent-hover', '--accent-2', '--accent-light', '--accent-glow']

export function applyBranding(branding) {
  if (!branding || !branding.accent_color) {
    clearBranding()
    return
  }
  const c = branding.accent_color
  const root = document.documentElement.style
  root.setProperty('--accent', c)
  root.setProperty('--accent-hover', `color-mix(in srgb, ${c}, black 18%)`)
  root.setProperty('--accent-2', c)
  root.setProperty('--accent-light', `color-mix(in srgb, ${c} 8%, transparent)`)
  root.setProperty('--accent-glow', `color-mix(in srgb, ${c} 35%, transparent)`)
}

export function clearBranding() {
  const root = document.documentElement.style
  ACCENT_VARS.forEach((v) => root.removeProperty(v))
}
```

- [ ] **Step 2: Crear `branding.api.js`**

`frontend/src/api/branding.api.js`:

```javascript
import { http } from "./http";

export async function getBranding() {
  return (await http.get("/api/branding/")).data;
}

export async function saveBranding(payload) {
  // payload: FormData (con logo) o objeto plano
  return (await http.put("/api/branding/", payload)).data;
}

export async function getPublicBranding(slug) {
  return (await http.get(`/api/public/branding/${slug}/`)).data;
}
```

- [ ] **Step 3: Aplicar branding en `auth.store.js`**

Modificar `loadMe` para aplicar branding tras cargar el usuario:

```javascript
import { applyBranding, clearBranding } from "../composables/useBranding";
```

```javascript
    async loadMe() {
      try {
        const res = await http.get("/api/me/");
        this.user = res.data;
        applyBranding(res.data?.branding);
      } catch (e) {
        this.user = null;
      } finally {
        this.loaded = true;
      }
    },
```

Y en `logout`, tras limpiar el user, agregar `clearBranding();` para revertir a la marca del producto.

- [ ] **Step 4: TopBar muestra logo + display_name**

En `frontend/src/components/AppTopBar.vue`, reemplazar el bloque `.brand`:

```html
      <div class="brand">
        <img v-if="brandLogo" :src="brandLogo" alt="" class="brand-logo" />
        <span v-else class="brand-mark">AS</span>
        <span class="brand-name">{{ brandName }}</span>
      </div>
```

En `<script setup>`, agregar computeds:

```javascript
const brandLogo = computed(() => auth.user?.branding?.logo_url || null)
const brandName = computed(() => auth.user?.branding?.display_name || 'AllSafe')
```

Y en `<style scoped>` agregar:

```css
.brand-logo { height: 24px; width: auto; max-width: 120px; object-fit: contain; display: block; }
```

- [ ] **Step 5: Verificación manual (build)**

Run: `cd frontend && npm run build`
Expected: build sin errores.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/composables/useBranding.js frontend/src/api/branding.api.js frontend/src/stores/auth.store.js frontend/src/components/AppTopBar.vue
git commit -m "feat(frontend): useBranding + branding en TopBar y carga de sesión"
```

---

### Task 6: Front — pantalla admin `/admin/branding`

**Files:**
- Create: `frontend/src/views/dashboards/AdminBranding.vue`
- Modify: `frontend/src/router/index.js`

**Interfaces:**
- Consumes: `getBranding`, `saveBranding` (Task 5), `auth.user.branding`, `applyBranding`.
- Produces: ruta `admin-branding` en `/admin/branding`.

- [ ] **Step 1: Crear `AdminBranding.vue`**

`frontend/src/views/dashboards/AdminBranding.vue` — pantalla con color picker (input color + hex), upload de logo con preview, toggle tema por defecto, campo nombre visible, botón guardar. Estado de upsell si el plan es Free (detectado por 403 al guardar o por `getSubscription`). Copys en voseo, estética hairline. Estructura mínima:

```html
<template>
  <div class="branding-page">
    <header class="head">
      <div>
        <p class="eyebrow">Personalización</p>
        <h1>Branding de tu organización</h1>
      </div>
    </header>

    <div v-if="locked" class="upsell">
      El branding está disponible en los planes <strong>Pro</strong> y <strong>Business</strong>.
      <router-link :to="{ name: 'admin-suscripcion' }">Ver planes →</router-link>
    </div>

    <form v-else class="form" @submit.prevent="onSave">
      <label class="field">
        <span class="field-label">Nombre visible</span>
        <input v-model="displayName" placeholder="p. ej. Acme Support" />
      </label>

      <label class="field">
        <span class="field-label">Color de acento</span>
        <div class="color-row">
          <input type="color" v-model="accentColor" />
          <input v-model="accentColor" class="hex" placeholder="#0038FF" />
        </div>
      </label>

      <label class="field">
        <span class="field-label">Logo (PNG/JPEG/WebP, máx. 512 KB)</span>
        <input type="file" accept="image/png,image/jpeg,image/webp" @change="onFile" />
        <img v-if="logoPreview" :src="logoPreview" class="preview" alt="" />
      </label>

      <label class="field-check">
        <input type="checkbox" v-model="defaultDark" />
        <span>Tema oscuro por defecto para usuarios nuevos</span>
      </label>

      <div v-if="error" class="error-msg">{{ error }}</div>
      <div v-if="okMsg" class="ok-msg">{{ okMsg }}</div>

      <button type="submit" :disabled="saving" class="btn-submit">
        {{ saving ? "Guardando..." : "Guardar branding" }}
      </button>
    </form>
  </div>
</template>

<script setup>
import { ref, onMounted } from "vue";
import { getBranding, saveBranding } from "../../api/branding.api";
import { applyBranding } from "../../composables/useBranding";
import { useAuthStore } from "../../stores/auth.store";

const auth = useAuthStore();
const displayName = ref("");
const accentColor = ref("#0038FF");
const defaultDark = ref(false);
const logoFile = ref(null);
const logoPreview = ref(null);
const locked = ref(false);
const saving = ref(false);
const error = ref("");
const okMsg = ref("");

onMounted(async () => {
  try {
    const b = await getBranding();
    displayName.value = b.display_name || "";
    accentColor.value = b.accent_color || "#0038FF";
    defaultDark.value = b.default_dark || false;
    logoPreview.value = b.logo_url || null;
  } catch (e) { /* defaults */ }
});

function onFile(ev) {
  const f = ev.target.files?.[0];
  logoFile.value = f || null;
  logoPreview.value = f ? URL.createObjectURL(f) : logoPreview.value;
}

async function onSave() {
  saving.value = true; error.value = ""; okMsg.value = "";
  try {
    const fd = new FormData();
    fd.append("display_name", displayName.value);
    fd.append("accent_color", accentColor.value);
    fd.append("default_dark", defaultDark.value);
    if (logoFile.value) fd.append("logo", logoFile.value);
    const b = await saveBranding(fd);
    applyBranding(b);
    if (auth.user) auth.user.branding = b;
    okMsg.value = "Branding actualizado.";
  } catch (e) {
    if (e?.response?.status === 403) locked.value = true;
    error.value = e?.response?.data?.detail
      || Object.values(e?.response?.data || {}).flat()[0]
      || "No se pudo guardar.";
  } finally {
    saving.value = false;
  }
}
</script>
```

(El estilo puede seguir el patrón hairline de `AdminMembers.vue`/`AdminSubscription.vue`; reusar clases `.eyebrow`, `.field`, `.btn-submit`, `.error-msg`, `.ok-msg` con `<style scoped>` propio.)

- [ ] **Step 2: Registrar la ruta**

En `frontend/src/router/index.js`, importar y agregar la ruta admin:

```javascript
import AdminBranding from "../views/dashboards/AdminBranding.vue";
```

```javascript
  { path: "/admin/branding", name: "admin-branding", component: AdminBranding, meta: { role: "ADMIN" } },
```

- [ ] **Step 3: Verificación (build)**

Run: `cd frontend && npm run build`
Expected: build sin errores.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/views/dashboards/AdminBranding.vue frontend/src/router/index.js
git commit -m "feat(frontend): pantalla admin /admin/branding (color, logo, tema, upsell)"
```

---

### Task 7: Front — pre-auth branded por slug (`/o/:slug/login` + invitación)

**Files:**
- Create: `frontend/src/views/public/OrgLoginView.vue`
- Modify: `frontend/src/router/index.js`
- Modify: `frontend/src/views/public/AcceptInvitationView.vue`

**Interfaces:**
- Consumes: `getPublicBranding(slug)` (Task 5), `applyBranding`/`clearBranding`, `LoginView` existente.
- Produces: rutas `/o/:slug/login` (name `org-login`) y `/o/:slug/invitacion/:token` (name `org-invitacion`). Caída a marca del producto en 404.

- [ ] **Step 1: Crear `OrgLoginView.vue`**

Envuelve el login existente aplicando branding por slug antes de pintar:

```html
<template>
  <LoginView :brand-name="brandName" :brand-logo="brandLogo" />
</template>

<script setup>
import { ref, onMounted, onUnmounted } from "vue";
import { useRoute } from "vue-router";
import LoginView from "../LoginView.vue";
import { getPublicBranding } from "../../api/branding.api";
import { applyBranding, clearBranding } from "../../composables/useBranding";

const route = useRoute();
const brandName = ref("AllSafe");
const brandLogo = ref(null);

onMounted(async () => {
  try {
    const b = await getPublicBranding(route.params.slug);
    brandName.value = b.display_name || "AllSafe";
    brandLogo.value = b.logo_url || null;
    applyBranding(b);
  } catch (e) { /* 404 -> marca producto */ }
});

onUnmounted(() => clearBranding());
</script>
```

Nota: si `LoginView.vue` no acepta props `brand-name`/`brand-logo`, agregar `defineProps({ brandName: { type: String, default: 'AllSafe' }, brandLogo: { type: String, default: null } })` y usarlos en su encabezado. Revisar el archivo antes de editar y adaptar.

- [ ] **Step 2: Registrar rutas pre-auth por slug**

En `frontend/src/router/index.js`, dentro del `children` de `PublicLayout` (o como rutas públicas), agregar:

```javascript
      { path: "o/:slug/login", name: "org-login", component: OrgLoginView, meta: { public: true } },
      { path: "o/:slug/invitacion/:token", name: "org-invitacion", component: AcceptInvitationView, meta: { public: true } },
```

E importar `OrgLoginView`. La vista de invitación por slug reusa `AcceptInvitationView` (que resuelve la org por el token); el slug solo se usa para el branding.

- [ ] **Step 3: Branding por slug en `AcceptInvitationView.vue`**

En `onMounted`, si `route.params.slug` está presente, pedir branding público y aplicarlo:

```javascript
import { getPublicBranding } from "../../api/branding.api";
import { applyBranding, clearBranding } from "../../composables/useBranding";
```

Dentro de `onMounted`, antes o después de cargar la invitación:

```javascript
  if (route.params.slug) {
    try {
      const b = await getPublicBranding(route.params.slug);
      applyBranding(b);
    } catch (e) { /* sin branding */ }
  }
```

Y agregar `onUnmounted(() => clearBranding())`.

- [ ] **Step 4: Verificación (build)**

Run: `cd frontend && npm run build`
Expected: build sin errores.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/views/public/OrgLoginView.vue frontend/src/router/index.js frontend/src/views/public/AcceptInvitationView.vue
git commit -m "feat(frontend): pre-auth branded por slug (/o/:slug/login e invitación)"
```

---

### Task 8 (OPCIONAL, última — no bloquea H4): Branding en emails

**Files:**
- Modify: plantillas de email de `accounts` (verificación e invitación).
- Modify: el servicio que arma/envía esos emails.

**Interfaces:**
- Consumes: `org.branding` (logo_url absoluto, accent_color) de la org destino.

- [ ] **Step 1:** Localizar las plantillas/servicio de email en `accounts/` (verificación e invitación). Identificar dónde se arma el HTML/contexto.

- [ ] **Step 2:** Inyectar en el contexto `brand_logo_url` y `brand_accent` desde `branding_payload(org, None)` (con URL absoluta construida a partir de `FRONTEND_BASE_URL`/`settings`, ya que no hay `request`). Si la org no tiene branding pago, usar la marca del producto.

- [ ] **Step 3:** Ajustar la plantilla para usar esos valores (header con logo, color del botón CTA).

- [ ] **Step 4:** Test: el email de invitación de una org con branding incluye el logo/color; una org Free usa la marca del producto.

- [ ] **Step 5: Commit**

```bash
git commit -m "feat(accounts): branding de la org en emails de verificación e invitación"
```

Si el tiempo aprieta, esta tarea se difiere sin afectar el cierre de H4.

---

## Cierre de la rama

Tras la última tarea no-opcional (Task 7) y la review final:

- [ ] **Gate en DB fresca:** `cd backend && C:/Python312/python.exe manage.py test --noinput` — toda la suite debe pasar (incluyendo las nuevas de tenancy).
- [ ] **Runbook:** documentar en `docs/deploy.md` (o equivalente) que el volumen de `MEDIA_ROOT` (`branding/logos/`) debe persistir y que en prod Nginx sirve `/media/`.
- [ ] Usar superpowers:finishing-a-development-branch para cerrar (merge local, la rama se mantiene local salvo indicación contraria).
